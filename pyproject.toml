[build-system]
requires = ["setuptools>=68,<81"]
build-backend = "setuptools.build_meta"

[project]
name = "http-security-headers-checker"
version = "0.3.0"
description = "Inspect common HTTP response security headers for one authorized public URL."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Oscar Opemba" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Information Technology",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]

[project.urls]
Repository = "https://github.com/Oscar-Opemba/http-security-headers-checker"
Issues = "https://github.com/Oscar-Opemba/http-security-headers-checker/issues"

[project.scripts]
http-security-headers-checker = "app:main"

[tool.setuptools]
py-modules = ["app", "common", "security_utils"]
